
document.addEventListener("DOMContentLoaded", () => {
  const app = document.getElementById("calendar-app");
  app.innerHTML = `
    <h1>Leave Booking System</h1>
    <p>This is the restored version with working logic coming in.</p>
    <p>Please wait while calendar loads...</p>
  `;
});
